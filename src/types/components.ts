/**
 * Shared component prop types
 * Only move types here when they're used across multiple components
 */

// Example shared component types (commented out by default)
// Uncomment and use these as needed

// export interface BaseProps {
//   className?: string
//   id?: string
// }

// export type Variant = 'primary' | 'secondary' | 'tertiary' | 'danger'
// export type Size = 'xs' | 'sm' | 'md' | 'lg' | 'xl' 